<section class="settings__section">
  <header>
    <h2 class="divider"><%= t(".title") %></h2>
    <div><%= t(".description_html") %></div>
  </header>

  <%= render "entropy/auto_close", model: account.entropy, url: account_entropy_path, disabled: !Current.user.admin? %>
</section>
